server:
  port: 8080

spring:
  application:
    name: webhook-service

  kafka:
    bootstrap-servers: localhost:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      acks: all
      retries: 3
      properties:
        enable.idempotence: true
        spring.json.add.type.headers: false
    consumer:
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      auto-offset-reset: earliest
      properties:
        spring.json.trusted.packages: "com.mphasis.webhook.model"
        spring.json.value.default.type: "com.mphasis.webhook.model.KafkaJiraEvent"

app:
  kafka:
    topic: jira.issue.events
  webhook:
    secret: ""   # set to a non-empty string to enable token validation
                 # then add ?token=<secret> to your Jira webhook URL
